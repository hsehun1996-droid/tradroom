# --- core ---
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=15.0.0          # parquet storage
python-dotenv>=1.0.0
pydantic>=2.6.0
pydantic-settings>=2.2.0

# --- korea market data (optional at runtime; system falls back to sample data) ---
pykrx>=1.2.8             # KRX prices / investor flows / short selling
finance-datareader>=0.9.50
OpenDartReader>=0.2.2    # DART filings / financials
requests>=2.32.0         # ECOS / FRED REST

# --- backtest / analytics ---
scipy>=1.12.0

# --- api ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# --- dev ---
pytest>=8.0.0
